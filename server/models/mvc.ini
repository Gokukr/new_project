final-project/
│
├── prisma/
│   └── schema.prisma        # Prisma schema for database models
│
├── controllers/
│   ├── adminController.js    # Handles admin-specific logic
│   ├── trainerController.js  # Handles trainer-specific logic
│   └── userController.js     # Handles user-specific logic
│
├── models/
│   └── index.js              # Initialize Prisma client
│
├── routes/
│   ├── adminRoutes.js        # Routes related to admin actions
│   ├── trainerRoutes.js      # Routes related to trainer actions
│   └── userRoutes.js         # Routes related to user actions
│
├── services/
│   ├── adminService.js       # Business logic for admin actions
│   ├── trainerService.js     # Business logic for trainer actions
│   └── userService.js        # Business logic for user actions
│
├── middlewares/
│   └── authMiddleware.js     # Authentication and role-based access control
│
├── app.js                     # Main app entry point (Express setup)
│
├── .env                        # Environment variables (DB connection, JWT secret, etc.)
├── package.json                # Project dependencies
└── server.js                   # Starts the Node.js server
